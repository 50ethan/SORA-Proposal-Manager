<!doctype html>
<html lang="ja">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ project.project_name }} | SORA Proposal Manager</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
</head>
<body>
    <header class="header">
        <div class="header-inner header-layout">
            <div>
                <h1>{{ project.project_name }}</h1>
                <p class="product-subtitle">{{ project.company_name }}</p>
                <p class="product-catch">案件詳細</p>
            </div>

            <a class="logout-button" href="/admin/">
                ダッシュボードへ戻る
            </a>
        </div>
    </header>

    <main class="wrap">
        <section class="card">
            <div class="section-heading">
                <div>
                    <p class="section-kicker">PROJECT OVERVIEW</p>
                    <h2>案件概要</h2>
                </div>
            </div>

            <div class="project-detail-grid">
                <div>
                    <span>顧客</span>
                    <strong>{{ project.company_name }}</strong>
                </div>

                <div>
                    <span>案件名</span>
                    <strong>{{ project.project_name }}</strong>
                </div>

                <div>
                    <span>予定金額</span>
                    <strong>¥{{ "{:,}".format(project.amount or 0) }}</strong>
                </div>

                <div>
                    <span>営業ステータス</span>
                    <strong>{{ project.status }}</strong>
                </div>

                <div>
                    <span>登録日</span>
                    <strong>{{ project.created_at }}</strong>
                </div>

                <div>
                    <span>更新日</span>
                    <strong>{{ project.updated_at }}</strong>
                </div>
            </div>
        </section>

        <section class="card">
            <h2>案件メモ</h2>

            {% if project.description %}
                <p class="project-detail-description">
                    {{ project.description }}
                </p>
            {% else %}
                <div class="empty-state">
                    案件メモはありません。
                </div>
            {% endif %}
        </section>

        <section class="card">
            <div class="section-heading">
                <div>
                    <p class="section-kicker">DOCUMENTS</p>
                    <h2>案件資料</h2>
                </div>
            </div>

            <div class="project-document-grid">
                <article class="project-document-card">
                    <div class="project-document-icon">📄</div>
                    <strong>HTML提案書</strong>
                    <p>案件ごとの提案内容をWeb形式で公開します。</p>

                    {% set proposal_document = documents_by_type.get("proposal") %}

                    <div class="project-document-actions">
                        {% if proposal_document %}
                            <a
                                class="secondary-button"
                                href="/admin/project/{{ project.id }}/document/{{ proposal_document.id }}/open"
                                target="_blank"
                                rel="noopener noreferrer"
                            >
                                開く
                            </a>
                        {% else %}
                            <button
                                type="button"
                                class="secondary-button"
                                disabled
                            >
                                開く
                            </button>
                        {% endif %}
                    </div>

                    <form
                        class="project-document-upload"
                        method="post"
                        action="/admin/project/{{ project.id }}/proposal/upload"
                        enctype="multipart/form-data"
                    >
                        <input
                            type="file"
                            name="file"
                            accept=".html,.htm,text/html"
                            required
                        >

                        <button type="submit">
                            {% if proposal_document %}
                                差し替える
                            {% else %}
                                アップロード
                            {% endif %}
                        </button>
                    </form>
                </article>

                <article class="project-document-card">
                    <div class="project-document-icon">🏢</div>
                    <strong>HTML会社案内</strong>
                    <p>会社情報・実績・サービスをWeb形式で表示します。</p>

                    {% set company_document = documents_by_type.get("company") %}

                    <div class="project-document-actions">
                        {% if company_document %}
                            <a
                                class="secondary-button"
                                href="/admin/project/{{ project.id }}/document/{{ company_document.id }}/open"
                                target="_blank"
                                rel="noopener noreferrer"
                            >
                                開く
                            </a>
                        {% else %}
                            <button
                                type="button"
                                class="secondary-button"
                                disabled
                            >
                                開く
                            </button>
                        {% endif %}
                    </div>

                    <form
                        class="project-document-upload"
                        method="post"
                        action="/admin/project/{{ project.id }}/company/upload"
                        enctype="multipart/form-data"
                    >
                        <input
                            type="file"
                            name="file"
                            accept=".html,.htm,text/html"
                            required
                        >

                        <button type="submit">
                            {% if company_document %}
                                差し替える
                            {% else %}
                                アップロード
                            {% endif %}
                        </button>
                    </form>
                </article>

                <article class="project-document-card">
                    <div class="project-document-icon">💰</div>
                    <strong>PDF見積書</strong>
                    <p>正式な見積書をPDFで登録・共有します。</p>

                    {% set quotation_document =
                        documents_by_type.get("quotation") %}

                    <div class="project-document-actions">
                        {% if quotation_document %}
                            <a
                                class="secondary-button"
                                href="/admin/project/{{ project.id }}/document/{{ quotation_document.id }}/open"
                                target="_blank"
                                rel="noopener noreferrer"
                            >
                                開く
                            </a>
                        {% else %}
                            <button
                                type="button"
                                class="secondary-button"
                                disabled
                            >
                                開く
                            </button>
                        {% endif %}
                    </div>

                    <form
                        class="project-document-upload"
                        method="post"
                        action="/admin/project/{{ project.id }}/quotation/upload"
                        enctype="multipart/form-data"
                    >
                        <input
                            type="file"
                            name="file"
                            accept=".pdf,application/pdf"
                            required
                        >

                        <button type="submit">
                            {% if quotation_document %}
                                差し替える
                            {% else %}
                                アップロード
                            {% endif %}
                        </button>
                    </form>
                </article>

                <article class="project-document-card">
                    <div class="project-document-icon">📜</div>
                    <strong>PDF契約書</strong>
                    <p>契約書をPDFで登録・共有します。</p>

                    <div class="project-document-actions">
                        <button type="button" class="secondary-button" disabled>
                            開く
                        </button>
                        <button type="button" disabled>
                            アップロード
                        </button>
                    </div>
                </article>
            </div>
        </section>

        <section class="card">
            <div class="section-heading">
                <div>
                    <p class="section-kicker">REGISTERED DOCUMENTS</p>
                    <h2>登録済み資料</h2>
                </div>
            </div>

            {% if documents %}
                <div class="registered-document-list">
                    {% for document in documents %}
                        <article class="registered-document-item">
                            <div>
                                <span class="document-type-label">
                                    {% if document.document_type == "proposal" %}
                                        HTML提案書
                                    {% elif document.document_type == "company" %}
                                        HTML会社案内
                                    {% elif document.document_type == "quotation" %}
                                        PDF見積書
                                    {% elif document.document_type == "contract" %}
                                        PDF契約書
                                    {% else %}
                                        その他資料
                                    {% endif %}
                                </span>

                                <strong>{{ document.title }}</strong>

                                <p>
                                    更新日時：{{ document.updated_at }}
                                </p>
                            </div>

                            <div class="registered-document-actions">
                                <span class="document-mime">
                                    {{ document.mime_type }}
                                </span>

                                <a
                                    class="secondary-button"
                                    href="/admin/project/{{ project.id }}/document/{{ document.id }}/open"
                                    target="_blank"
                                    rel="noopener noreferrer"
                                >
                                    開く
                                </a>
                            </div>
                        </article>
                    {% endfor %}
                </div>
            {% else %}
                <div class="empty-state">
                    この案件には、まだ資料が登録されていません。
                </div>
            {% endif %}
        </section>
    </main>
</body>
</html>
